---
import { useTranslations } from '../i18n';
import Layout from './Layout.astro';
import '../styles/features.css';

interface Props {
  title?: string;
  description?: string;
  theme?: string;
  frontmatter?: {
    title?: string;
    description?: string;
    theme?: string;
  };
}

const t = useTranslations();
const frontmatter = Astro.props.frontmatter || {};
const title = Astro.props.title || frontmatter.title || t.common.siteTitle;
const description =
  Astro.props.description ||
  frontmatter.description ||
  t.common.siteDescription;
const theme = Astro.props.theme || frontmatter.theme || 'dark';
---

<Layout title={title} description={description} theme={theme}>
  <main class="page">
    <slot />
  </main>
</Layout>
